<p>Сборник задач по JS с возможностью проверки результатов.</p>
<p>Задачи разбиты по разделам (см. меню выше).</p>
<p>Решение каждой задачи являет собой функцию, которая должна быть объявлена в месте, указанном в условии. Например, если написано, что решение должно быть в <code>some/path/to/file.js#coolFunc</code>, то в файле <code>some/path/to/file.js</code> (см. от корня проекта) необходимо найти <code>coolFunc</code> и подставить на ее место свой код. В начале все функции записаны в виде - <code>coolFunc: Ember.B</code> (просто заглушки). Вам нужно сделать - <code>coolFunc: function () { /* some your code */}</code>.</p>
<p>Для каждой задачи уже написан свой комплект тестов. Запустить их можно нажав по ссылке <code>проверить тесты</code> (в блоке с условием для каждой задачи). Откроется новая вкладка, где будут эти тесты (запускаются автоматически). Если все они будут зеленые, поздравляю - скорее всего задача решена правильно. Если же какие-то тесты будут красными, то обратите внимание на то, какие параметры принимала функция в тесте, и какой результат ждал от нее тест.</p>