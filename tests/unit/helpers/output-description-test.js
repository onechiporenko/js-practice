import { outputDescription } from '../../../helpers/output-description';
import { module, test } from 'qunit';

module('Unit | Helper | output description');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = outputDescription(42);
  assert.ok(result);
});
