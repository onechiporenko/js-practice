import { linkToTests } from '../../../helpers/link-to-tests';
import { module, test } from 'qunit';

module('Unit | Helper | link to tests');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = linkToTests(42);
  assert.ok(result);
});
