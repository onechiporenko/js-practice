import { controllerMethod } from '../../../helpers/controller-method';
import { module, test } from 'qunit';

module('Unit | Helper | controller method');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = controllerMethod(42);
  assert.ok(result);
});
